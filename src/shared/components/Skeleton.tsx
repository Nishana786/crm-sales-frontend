import { View } from "react-native";

export default function Skeleton() {
  return (
    <View
      style={{
        height: 80,
        backgroundColor: "#e5e7eb",
        borderRadius: 10,
        marginBottom: 10,
      }}
    />
  );
}